<div class="container">
  <h2>Favourite Video List</h2>
  <% if current_user.id && current_user.user_favourite_videos.present? %>
    <div class="row">
      <div class="col-12 col-md-11 mx-md-auto col-lg-9 mx-lg-auto">
        <% @videos.each do |video| %>
          <% if @user_fav_videos.include?(video.id) %>
            <div class="card shadow rounded-0 mt-3">
              <div class="card-body">
                <div class="row">
                  <div class="col-12 col-md-4 col-lg-3">
                    <iframe width="100%" height="200"
                    src="<%= video.yt_url %>">
                    </iframe>
                  </div>
                  <div class="col-12 col-md-8 col-lg-9">
                    <h3><%= link_to video.title, 'javascript:void(0)' %></h3>
                    <p>It is a long established fact that a reader will be distracted by the readable content of a page when looking at its layout. The point of using Lorem Ipsum is that it has a more-or-less normal distribution.</p>            
                  <%= link_to "Back", videos_path, class: 'btn btn-light btn-lg mt-4' %>
                  </div>
                </div>
              </div>
            </div>          
          <% end %>
        <% end %>
      </div>
    </div>
  <% else %>
  <h2>No data available please Add video as favourite.</h2>
  <%= link_to "Back", videos_path, class: 'btn btn-light btn-lg mt-4' %>
  <% end %>
  </div>